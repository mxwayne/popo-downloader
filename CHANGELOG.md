# 更新记录

## v0.7.9（2026-09-23）

- 收口下载恢复与重试：页面不可用恢复有总次数和时长上限，重试只继续未完成文件，并按 Gopeed 任务身份对账避免重复建任务。
- 区分 POPO 权限拒绝、页面恢复、Gopeed 网络中断和响应契约异常；补充脱敏诊断字段。
- Gopeed 自动配对 API Token：Gopeed 自行生成并保存密钥，本机助手通过 Native Messaging 配对并以 Windows DPAPI 保护；不再要求用户在 Gopeed 与扩展设置中手动复制。

## v0.7.8（2026-08-28）

- 修复 POPO 页面标题缺少文件后缀时，ZIP 下载结果被保存为普通无后缀文件的问题；现在会从服务器下载地址与 Content-Disposition 恢复真实后缀。
- 已有 `.zip` 或 `.ZIP` 的名称不会重复追加为 `.zip.zip`，重试和重新下载继续使用同一个正确文件名，非 ZIP 与真正无后缀文件不受影响。
- 固化仓库源码到隔离 Dev Extension 的安全同步流程；Dev 名称、扩展身份和目录与 Stable 保持分离，正式发布包仍只来自仓库的 Stable 构建链路。

## v0.7.7（2026-08-26）

- 延续面向普通用户的 Single EXE 安装入口、Windows PE Version metadata、Bootstrapper TEMP 安全清理和与正式 ZIP 完全一致的内嵌 payload。
- Stable 自动更新仍使用签名 ZIP，`latest.json` 不指向 EXE，并保持从 0.7.5 到 0.7.7 的更新兼容合同。
- 修复 Windows CI 中维护模式事务测试的同步竞态：测试现在通过显式 helper readiness 和独立 observer，等待 maintenance marker 激活后 Native Host registration 真正进入暂停状态再固化证据；不改变 Setup、Native Host 或 Gopeed 的生产行为。

## v0.7.6（2026-08-26）

- 新增面向普通用户的 Single EXE 安装入口；EXE 内嵌并使用与正式 ZIP 完全相同的 release payload，继续保留 ZIP 自动更新兼容性，`latest.json` 仍只指向 ZIP。
- Bootstrapper 在解包前校验内嵌 payload 的 SHA-256，并只负责释放 payload、调用现有 `POPO-Setup.exe` 和透传退出码，不重写安装事务、更新协议或回滚机制。
- 安装完成、用户取消或 payload 校验失败后会安全清理本轮 `POPO-Installer-{GUID}`；修复 Windows 瞬时 sharing violation 导致空 TEMP 根残留的问题，采用有限退避重试并记录最终异常。
- 正式构建和 Release Verify 新增 EXE、EXE SHA-256、ZIP/内嵌 payload 一致性及安全 cleanup Gate；GitHub Release assets 增加 EXE 与独立 SHA-256，稳定自动更新载体仍为 ZIP。
- Single EXE 写入并严格验证 Windows PE 版本元数据：`FileVersion` 使用产品版本对应的四段版本（如 `0.7.6.0`），`ProductVersion` 与正式产品版本一致（如 `0.7.6`）；Release Verify 会阻止版本信息缺失、为 `0.0.0.0` 或与正式版本不一致的 EXE 进入发布。
- 保持普通用户无管理员权限安装，并兼容 0.7.5 的正式 Extension 路径、Gopeed 数据、设置和下载记录；Single EXE 不参与自动升级。

## v0.7.5（2026-08-25）

- 修复绿色版覆盖更新和迁移时，普通用户无法创建登录启动任务会导致整笔安装回滚的问题：优先使用计划任务；遇到拒绝访问、超时或启动定义校验失败时，自动回退为当前用户 `Run` 启动项。
- 覆盖、迁移、回滚和卸载会识别并清理计划任务与当前用户 `Run` 两种启动项，避免留下重复的更新 Agent。
- 安装失败窗口改为简短中文原因；同时在 `%LOCALAPPDATA%\POPOStableDownloader\Logs\setup.log` 保留脱敏的完整异常链，即使回滚后仍可读取。
- 弹窗读到上次“更新检查失败”时会立即重试，服务恢复后自动回到“已是最新版”；连续失败会限频，不会重复请求。
- 安装事务测试使用独立注册表沙箱，不再覆盖本机正式版安装位置、Native Host 或启动项。
- 正式包必须内置经校验的官方 Sentry 诊断接收地址；未配置时打包直接失败，包验证也会拒绝无法自动回传错误的成品。
- 修复正式版安装、覆盖更新或修复时，扩展会在用户退出 Gopeed 后立刻将其重新拉起、导致只能抢一秒完成安装的问题：安装事务会先进入维护模式并暂停自动启动通道，等待 Gopeed 正常退出后继续；成功恢复新版连接，失败恢复原连接。
- 安装首页会自动选择唯一操作：未安装时安装，检测到旧版本时覆盖升级，版本相同时修复，更换位置时迁移；移除容易混淆的手动修复复选框。
- 精简安装窗口和完成提示：产品名只显示一次，版本只保留一行，升级时显示“旧版 → 新版”，安装、升级、修复或迁移动作只出现在主按钮上。

## v0.7.4（2026-08-24）

- 再次下载同一 POPO 文件夹时，逐文件核对 Gopeed 成功记录；同一 POPO 素材即使本地目录层级发生变化也会直接跳过，不再创建可能带 `(1)` 后缀的重复文件。
- 查重结果现在还会核对实际文件是否存在且大小一致；Gopeed 留有旧完成记录、但本地文件已删除或移走时，不再误显示“已下载”，而是重新下载缺失文件。
- 下载完成结果保留 2 分钟后自动收起为绿色对号；下载按钮恢复可用，再次点击会重新查重，已有文件跳过、缺少文件继续下载。
- 修复再次查重时偶发一次未定位到文件夹就直接显示“未能开始”的问题；现在会自动重新查找，连续失败后才停止任务。
- 修复切换 POPO 目录时过渡空状态与旧列表短暂并存、导致按钮悬空闪现和“正在统计”卡顿的问题；现在会在 POPO 原生双击完整触发后显示统一加载骨架，遮住原生白屏和残留提示框，目标目录稳定后一次性恢复按钮，异常切换超过 5 秒会自动退出过渡层。

## v0.7.3（2026-08-19）

- 新增下载停滞、Gopeed 任务失联和后台异常的自动诊断；诊断会先脱敏，断网时在本机限量排队，恢复后自动重试。
- 扩展弹窗新增“诊断与回传”，支持立即发送和复制诊断状态；Sentry 接收链路、IP 禁止存储和敏感字段脱敏均已完成实测。
- 新增更新 Agent 阶段一影子桥接，严格校验本机进程、端口和协议并与旧签名更新结果对比；旧更新链路仍是正式主路径。
- 修复浏览器拒绝跨域请求并中断连接时可能带停更新 Agent 的问题；单个客户端断线现在只记录诊断，不影响后续合法请求。

## v0.7.2（2026-08-12）

- 修复手动下载在接口权限拒绝后无法恢复单文件地址的问题。
- 修复一键下载中某个文件夹未完成后，后续队列被自动暂停的问题。
- 失败任务继续保留重试入口，同时不会阻塞后续排队任务。

## v0.7.0（2026-08-11）

- 首个正式绿色版，保留 beta.11 已完成的稳定下载、任务恢复、事务安装和回滚能力。
- 新增腾讯云 COS 正式通道自动更新：仅在没有活动下载任务时安装，完成后自动重新加载扩展。
- 更新清单与安装包使用 RSA-SHA256 发布签名和 SHA-256 双重校验，拒绝伪造或损坏的远程版本。

## v0.7.0-beta.11（2026-08-06）

- P0 引入 Gopeed 官方 SDK、TypeScript + Zod 运行时契约和 XState 状态迁移约束，减少接口漂移、异常数据和非法状态跳转。
- P1 将大任务文件明细迁移到 IndexedDB + idb，并补充万级文件、断网恢复、弹窗刷新和浏览器重启自动化测试。
- 将扩展弹窗、页面项目数、文件夹下载按钮、左下任务摘要和右下事件通知迁移到 React + TypeScript，并保持页面虚拟列表适配。
- 固化三个界面的职责与防重复规则：弹窗负责完整管理，左下只显示持续摘要，右下只显示完成、失败或服务断开等一次性事件。
- 绿色安装器改为候选版本完整校验后再切换；失败自动恢复旧版本，成功后保留上一版本回滚快照。
- 修复恢复任务在原 POPO 标签页已经关闭时仍绑定旧标签页、最终以 0/0 失败的问题；从弹窗恢复时改用当前打开的 POPO 页面承载隐藏工作区。
- 恢复尝试在扫描前失败后，原取消任务会重新显示“恢复未开始文件”，可再次恢复且继续按 Gopeed 历史跳过已完成文件。
- 失败任务不再显示移动中的扫描进度条，并直接显示实际失败原因。
- 弹窗版本号改为读取扩展清单，不再长期错误显示 beta.6。

## v0.7.0-beta.10（2026-08-04）

- 修复旧版任务在取消时未保留文件明细，导致“恢复未开始文件”按钮报无可恢复文件的问题。
- 旧任务恢复时会读取 Gopeed 已有的完成、进行中和暂停任务，按最终保存路径跳过已开始文件，只重新下载缺失部分。
- 保持递归下载全部用户文件格式，不按扩展名筛选。

## v0.7.0-beta.9（2026-08-04）

- 新增“恢复未开始文件”：误取消后保留已完成和已交给 Gopeed 的文件，只重新扫描并处理被取消的剩余文件。
- 兼容 beta.6 已经保存的取消状态，升级后可直接恢复，不必从头重新下载整个文件夹。
- 修复暂停后关闭原页面、在其他 POPO 团队空间重新打开时无法继续的问题；继续下载会主动重建页面工作区。
- 新增误取消恢复、跨团队空间恢复和继续时重建工作区的回归测试。

## v0.7.0-beta.8（2026-08-03）

- 修复下载过程中打开扩展弹窗时，连接检查可能覆盖较新的任务状态，导致进度停在一个 Gopeed 已完成的文件上的问题。
- 后台再次运行时会自动恢复遗留在“准备中”的文件，不再永久停住。
- 新增弹窗轮询不写入任务状态、准备步骤中断自动接续的回归测试。

## v0.7.0-beta.7（2026-08-03）

- 修复旧版本保存过的文件格式或关键词筛选仍在后台生效、导致文件夹内其他类型文件被跳过的问题。
- 文件夹下载现在固定递归处理所有用户文件，不限制扩展名，也包含无扩展名文件；仅忽略系统自动生成的元数据文件。
- 新增升级场景与混合文件格式回归测试。

## v0.7.0-beta.6（2026-08-03）

- 修复取消任务重新打开后仍停在等待状态的问题；取消会立即结束 POPO 队列记录，同时保留已经交给 Gopeed 的文件，并继续下一个排队任务。
- 已完成和已取消任务不再长期显示在扩展弹窗中，刷新网页或重新打开扩展后只保留进行中的任务。
- 尚未解决的失败仍会保留重试入口；同一文件夹重新下载或重试后，旧失败提示会自动被新结果替代。
- 修复扩展后台短暂中断后，旧连接报错一直留在弹窗中的问题。

## v0.7.0-beta.5（2026-08-03）

- 修复项目数被插入右侧排序工具栏、导致显示在工具栏上方的问题。
- 项目数现在固定显示在左侧“所有类型”筛选按钮右边，并保持右侧排序工具栏原有的单行布局。
- 新增定位锚点与左侧自动占位检查，防止后续改动再次把项目数推到页面顶部。
- 绿色包 SHA-256：`42ce99e5c853bbdc14e480566aedd3f9fc35ca6758288d81ba066a5ee0cd8b1f`。

## v0.7.0-beta.4（2026-08-03）

- 修复文件夹页缺少当前目录项目总数、用户无法在下载前判断目录规模的问题。
- 在“按照目录顺序”左侧显示当前第一层“文件 + 文件夹”的项目数；虚拟大列表通过行号与占位高度计算，不滚动用户页面。
- 移除“发现 N 个文件，确认下载？”手动确认窗口；点击下载后直接入队，扫描完成后自动连接 Gopeed 开始下载。
- 队列新增“当前层项目数”口径，并将递归发现的文件和文件夹分开显示，避免数量含义混淆。
- 空目录、SPA 目录切换、34 项虚拟大列表、直接入队与无确认弹窗均纳入自动化和人工验收。
- 绿色包 SHA-256：`245dc3e7d9a94a4d9a32fbe8e89359648b1cb6754633feff9eb6ed8cf93e226d`。

## v0.7.0-beta.3（2026-08-03）

- 修复把 POPO 页面空间短码误当作真实团队空间 ID，导致预览未直接暴露媒体地址时批量出现“下载未建立”的问题。
- 单文件地址接口未返回地址时会短暂重试 3 次，并在最终失败信息中保留 HTTP、接口代码和消息，便于继续定位。
- 刷新临时地址时若 Gopeed 原任务已不存在，自动建立新任务，不再反复返回 `task not found`。
- 原 POPO 标签页关闭后，可在重新打开的同一团队空间页面自动接管未完成任务，不再永久停在等待状态。

## v0.7.0-beta.2（2026-08-03）

- 左下角队列改为默认折叠，只显示进行中任务；完成和失败历史仅在扩展弹窗保留。
- 页面刷新后自动重建隐藏工作区；已开始的 Gopeed 下载继续，正在准备的文件退回队列且不计失败次数。
- 页面队列和扩展弹窗增加文件进度条，固定扩展图标显示百分比、排队数或失败提醒。
- 修复队列按钮与全页 DOM 监听互相触发、导致 POPO 文件列表消失的问题。
- 绿色包 SHA-256：`1505235bce37e3080c502ad3aad158b6a6660322a71353e6a7f4c621f8905f81`。

## v0.7.0-beta.1（2026-08-03）

- 支持连续添加多个母文件夹，并在页面左下角和扩展弹窗显示下载队列。
- 同一文件夹重复点击不会创建重复任务，会提示当前处理或排队位置。
- 每个任务可以取消未开始文件；已经交给 Gopeed 的文件会保留并继续完成。
- 修复取消与异步下载准备并发时旧状态覆盖新状态、旧任务继续复活的问题。
- 大文件列表改为每 200 项分块保存，并申请大容量本地存储权限。
- 增加万级文件统计、三任务排队和安全取消测试。
- 绿色包 SHA-256：`6c88a3c8948c05905c69bffd0b3b7ff7aff86a187729a6693d64cd91a2779890`。

## v0.6.0-beta.3（2026-08-03）

- 提供 Windows x64 绿色 ZIP，自带 Gopeed v1.9.3 和首次配置助手。
- 固定 Chrome 扩展 ID，后续更新可覆盖原运行目录。
- 下载任务固定并发 5，支持暂停、继续、取消和失败重试。
- 自动测试 32/32 通过，隔离验证确认包内 Gopeed 可正常启动。
- 绿色包 SHA-256：`6ccb45700f7462f20d530f508fa3d06e770be4c5e9e3ac21e02da2cfdc3b175c`。

首次使用需解压 ZIP、运行 `POPO-Setup.exe`，并在 Chrome 中手动加载一次扩展。
