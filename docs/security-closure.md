# POPO 下载器问题与安全检查记录

检查日期：2026-09-23
范围：POPO 0.7.8 下载失败证据、扩展与本机 Gopeed 通信、任务恢复/重试、诊断上报、Windows Dev 验证与安装链路。Stable 未修改、未同步、未发布。

## 发现与必要性

| 优先级 | 问题与证据 | 核心原因 / 确认程度 | 必要性与处理方案 | 状态 |
|---|---|---|---|---|
| 高 | POPO 下载地址请求返回 HTTP 200，但 JSON `status: 405`、`message: 没有权限操作`。现场请求端点分别出现 `downloadAuth` 与网页原生 `page/download`；用户后来反馈同事下载异常已解决。 | 业务码 405 是服务端权限拒绝；由于现场对照的端点并不相同，现有截图不能证明请求头差异就是根因。用户报告问题已解决，但具体修复因素未确认。 | 保留 HTTP 状态与业务码的区分；对 405 不盲目重试，不拼接猜测参数或绕过服务端权限。若出现新复现，再在同账号同文件对比网页原生与扩展请求，并仅记录脱敏字段。 | 用户反馈同事下载异常已解决；本轮没有改动 405 取址逻辑，不把新版本模拟测试表述为该现场问题复测。 |
| 高 | 同一 JPG 每约 94 秒进入“页面刷新中、稍后自动继续”，超过 30 分钟；曾记录查找超时 89233ms。 | 已确认恢复等待逻辑可因反复工作区不可用持续重入；旧恢复轮次没有可靠的总时限/次数终止条件。截图显示症状，与此路径一致。 | 恢复次数上限为 8 次且总时长上限为 5 分钟；到限后清空待执行扫描队列、明确失败阶段和原因，保留已完成文件及未完成明细供重试。重试只选未完成项，并先按稳定文件标识与 Gopeed 对账，避免重复建任务。 | 已修复；本地恢复超限、保留已完成项和失败项可重试测试通过。Windows Dev 已同步该代码，真实页面恢复/超限 Smoke 尚待用户在 Dev 上验收。 |
| 高 | Gopeed SDK 返回 `meta.res: Invalid input: expected object, received null`，旧日志同时提示连接中断、重连或重启。 | 已确认响应元数据可在任务尚未生成时为 `null`；旧处理将契约异常与连接异常混为一类，可能错误触发离线/重启恢复。 | 接受契约允许的 `meta.res: null`；非法响应格式单独计数、暂停相关项目并提供明确诊断，不标记连接断开、不重启 Gopeed、不创建重复任务。真正网络中断继续走连接恢复路径。 | 已修复；`null` 合法、连续格式异常不会误报断线或重建任务的测试通过。 |
| 高 | Gopeed 本机 REST Token 默认为空时 API 无鉴权；REST CORS 允许 `*`。Gopeed v1.9.3 桌面程序将 `apiToken` 保存在自身 Hive 配置中。 | 空 Token 无鉴权为源码确认；恶意网页能否跨源访问目标浏览器回环 API 未实测。Gopeed 桌面版没有受支持的外部 REST/CLI 写入启动 Token 接口。 | 0.7.9 候选为随包 Gopeed v1.9.3 增加首次启动随机 Token：Gopeed 仍调用自己的配置持久化；Native Host 通过本机桥接获取并以当前 Windows 用户 DPAPI 加密保存；扩展通过 Native Messaging 自动配对。旧用户已有 Gopeed 任务存储原样迁移；不直接改 Hive 数据库，不把 Token 放在命令行、进程环境、日志或 Sentry。桥接文件在本机 Gopeed storage 中，由 Native Host 消费后删除。 | 已实现并在 Windows 候选包验证：首次启动自动生成和保存，未认证 API 返回 401，配对后返回 200，重启保留同一 Token，DPAPI 加解密往返通过。 |
| 中 | Gopeed 响应异常和下载失败需要可靠诊断；日志可能含文件名、路径、URL 或标识。 | 已确认上报管线只接收经过脱敏的上下文；下载拒绝事件记录失败步骤、HTTP 状态、业务码、尝试数和恢复数。独立代码审查发现脱敏器以前只检测字符串中是否有 `token=...` 标签，若调用方误传裸值形式的 `authorization` / `apiToken` 字段会被当普通文本保留；当前生产调用点没有把凭证放入 context，未发现已发生泄漏。 | 按字段名拒绝认证凭证、Cookie、API key 和签名/下载 URL 字段（字段名标准化后再匹配）；ID 哈希、路径/URL 文本脱敏保持。禁止 Cookie、Authorization 值和完整签名下载 URL 出现在 Sentry。 | 已修复；新增裸凭证与签名 URL 字段测试通过。未验证 Windows Dev 到 Sentry 的线上投递。 |
| 中 | 远程 Windows Dev 验证依赖传入主机、根目录、补丁和未跟踪文件归档。 | 先前检查确认脚本参数可进入 SSH/shell 与解包流程；不限制会扩大命令注入、错误目标写入及归档覆盖风险。 | 主机/路径严格白名单；patch、bundle 和归档校验 SHA-256；归档在空目录提取并拒绝符号链接、路径逃逸、tracked 文件冲突与覆盖。仅固定 Dev 目录可同步。 | 已加约束；Windows 完整验证和 Dev 同步通过。Stable 未触碰。 |
| 中 | Windows 安装验收脚本曾依赖打包名大小写、系统区域编码、安装器等待行为及不准确的安装状态字段。 | 已确认这些问题可造成验收误报，或清理/等待不符合预期；不能将验证脚本的失败误判为产品安装失败。 | 按 ZIP 实际路径大小写不敏感匹配；明确 UTF-8 读取；以安装器进程完成和真实安装状态核验；清理锁文件有限重试并输出实际缺失路径。 | 已修复并在 Windows Dev 安装验证通过。 |
| 低 | 文件名验收用本机 HTTP 服务曾让无令牌的 `/api/status` 返回 Gopeed task ID 与本机路径，且未校验请求 Host，POST 只检查页面内嵌随机令牌。 | 代码事实已确认；潜在利用需要服务正在运行且浏览器可被 DNS rebinding 诱导，不能泛化为远程漏洞。Host 任意匹配会让攻击页面读取其同源验收页和令牌。 | 服务只绑定 loopback；固定 Host 校验；状态接口也必须带令牌；任务创建请求还必须有与服务 origin 完全一致的 `Origin`。 | 已修复；测试覆盖外部 Host、无状态 API 令牌、缺失写请求 Origin 和拒绝非回环监听。 |
| 低 | 0.7.8 的扩展设置曾要求用户手工配置同一个 Gopeed API Token。 | 手动步骤易漏配，导致扩展请求 401；此前 Windows 已确认无/错 Token 返回 401。 | 0.7.9 候选移除扩展手工 Token 输入项，Gopeed 首次启动自动生成并保存 Token，Native Host 自动配对。 | 手工输入项已移除；自动配对模拟测试和 Windows 候选包首次启动/重启验证通过。 |
| 信息 | 当前锁文件依赖已知公告扫描。 | 2026-09-23 `npm audit`（全量及生产依赖）均报告 0 项已知漏洞；该结果不覆盖 NativeHost/Gopeed 二进制或尚未公开的漏洞。 | 保持锁文件并随常规依赖更新复查；不因本次扫描结果升级无关依赖。 | 已检查。 |

## 验证记录

- Mac：`npm run check:full` 通过；Node 测试 247 项中 210 通过、0 失败、37 项因 Windows 专属工具/环境跳过；Playwright Smoke 7 项通过、1 项跳过。随后本次构建脚本和许可文件调整通过 `npm run check`、定向 Gopeed/release 测试及 `git diff --check`。
- Windows：完整 Node 检查 246 项中 245 通过、0 失败、1 项因人工任务启动验收而跳过；Playwright Smoke 8/8 通过。重建 Gopeed v1.9.3 补丁便携包，最小 PATH 下 Token 启动验收 `GOPEED_TOKEN_BOOTSTRAP=PASS`，自动打包安装与扩展同步均通过。Dev 同步批次 `09-23 17:11:46`；扩展版本 `0.7.9-dev`，扩展 ID `folfhehnopknchpoaajfpboibbhnlanf`。扩展目标为 `D:\POPO\Dev\POPODevDownloader\Extension`。Stable 未修改、未同步、未发布。
- Windows Git for Windows：原 `C:\Program Files\Git` 缺少 `git-bash.exe`、mintty 和 `tmp`。使用 Git 官方 x64 `2.55.0.5` 安装包（Authenticode 签名有效）原位修复；入口、mintty、`/tmp`、`git --version` 和 `mkdir` 已验证恢复。开始菜单快捷方式目标存在；之后在交互 Session 1 检测到 `git-bash.exe`、`mintty.exe`、`bash.exe` 进程，桌面会话启动已确认。
- 尚待使用方确认：Dev 扩展已同步至 Windows，Candidate 安装器及自动配对已在该 Windows 主机验收通过；需在 Chrome 重载 **POPO Dev 下载助手**并刷新 POPO 页面，使浏览器加载新扩展脚本。无需人工复制或设置 Gopeed API Token。真实 POPO 页面下载与恢复上限仍由使用方按需验收；本地模拟测试不作为同事历史任务现场复测。Stable 未修改、未发布。
- 当前分支为本地 `main`，HEAD `79d48c5`；本轮 0.7.9 改动和本记录更新尚未提交或推送。

## 使用边界

模拟测试通过只证明对应代码路径；本文件引用的 405 现场结果来自用户提供的 2026-09-22 交接总结，未由本轮独立复测。若用户以后报告仍有新的 405，再对同账号同文件对照网页原生与扩展请求，隐藏所有 Cookie、Authorization 值和签名 URL，仅保留请求头名称、参数名称/非敏感 ID、HTTP/业务码及 Trace ID。
- 本机文件侧证据：在 Windows 常用下载目录找到同名 `舰船展示插图-雷火V055级巡洋舰-调整.psd`，大小 `267541957` bytes（约 `255.1 MiB`），文件头为 PSD 魔数 `8BPS`，修改时间 `2026-09-22 11:46:32`。该文件自身的时间和格式不能单独证明完整性；历史 405 修复后的现场下载结论以用户提供的 2026-09-22 交接总结为准，不再要求重试同事已确认成功的任务。
